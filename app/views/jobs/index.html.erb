<div class="col-md-12">
    <!-- <div class="pull-right">
    <%= link_to("New Job", new_job_path, class: "btn btn-primary") %>
  </div> -->
    <div class="dropdown clearfix pull-right">
        <button class="btn btn-default dropdown-toggle" type="button" id="dropdownMenuDivider" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
            Sort<span class="caret"></span>
        </button>
        <ul class="dropdown-menu" aria-labelledby="dropdownMenuDivider">
            <li>
                <%= link_to("Wage Lower First", jobs_path(:order => "by_lower_bound")) %>
            </li>
            <li>
                <%= link_to("Wage Upper First", jobs_path(:order => "by_upper_bound")) %>
            </li>
            <li>
                <%= link_to("Publish Date", jobs_path) %>
            </li>
        </ul>
    </div>
    <table class="table table-boldered table-striped custab">
        <thead>
            <tr>
                <td>Jobs</td>
                <td>Wage Lower</td>
                <td>Wage Upper</td>
                <td>Published Time</td>
            </tr>
        </thead>
        <tbody>
            <% @jobs.each do |job| %>
                <tr>
                    <td>
                        <%= link_to(job.title, job_path(job)) %>
                    </td>
                    <td>
                        <%= job.wage_lower_bound %>
                    </td>
                    <td>
                        <%= job.wage_upper_bound %>
                    </td>
                    <td>
                        <%= job.created_at %>
                    </td>
                    <!-- <td>
          <%= link_to("Edit", edit_job_path(job)) %> |
          <%= link_to("Destroy", job_path(job), :method => :delete, :data => { :confirm => "Are you sure?"}) %>
        </td> -->
                </tr>
            <% end %>
        </tbody>
    </table>
</div>
